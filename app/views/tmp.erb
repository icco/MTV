<p>This page will reload in 20 seconds. The rendering pipeline runs once a minute.</p>

<ul>
  <% @files.each do |file| %>
    <li><a href="<%= file.url %>"><%= file.name %></a> (<%= file.mtime %>)</li>
  <% end %>
</ul>
